#include <iostream>
using namespace std;
int n, order[9], a[9], b[9];
int main()
{
	int count = 0, min, sec;
	while (cin >> n && n != 0)
	{
		for (int i = 0; i < n; i++)
		{
			cin >> a[i];
			cin >> b[i];
			a[i] *= 60;
			b[i] *= 60;
			order[i] = i;
		}
		dow
	}
}